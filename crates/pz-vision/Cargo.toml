[package]
name = "pz-vision"
description = "Dependency-free fiducial detection, perspective correction and cell sampling for the Photonic Zero (PZ) optical protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["computer-vision", "homography", "fiducial", "barcode", "no-std"]
categories = ["computer-vision", "multimedia::images", "no-std"]

[features]
default = ["std"]
std = []

[dependencies]

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
