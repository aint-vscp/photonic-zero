# Rust
/target/
**/target/
*.pdb
*.rs.bk

# The library is published as crates, not as an application, so the lock file
# is committed for reproducible CI. Remove this line if that changes.
# Cargo.lock

# Output of `pz encode`
/pz-frames/
*.ppm

# Node / WebAssembly bindings
node_modules/
dist/
pkg/
*.tsbuildinfo
.pnpm-store/
*.tgz

# Built by `npm run build` from the Rust sources, and again by prepublishOnly,
# so a published tarball always matches the commit it was built from.
packages/js/src/pz.wasm
packages/js/LICENSE-MIT
packages/js/LICENSE-APACHE

# Python bindings
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.whl

# Java bindings
bindings/java/target/
*.class
*.jar

# C / C++ examples
*.o
*.a
*.so
*.dylib
*.dll
*.exe
!scripts/*.exe

# Editors and IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.fleet/

# Operating system noise
.DS_Store
Thumbs.db
desktop.ini

# web/lib/ is deliberately NOT ignored. Vercel deploys from git and has no
# Rust toolchain, so the wasm module has to be a committed artifact for the
# demo to be deployable from a plain static host.
